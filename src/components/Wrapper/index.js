import styled from 'styled-components';

import * as Spacing from 'styles/spacing';

const Wrapper = styled.div`
  margin: 0 auto;
  max-width: ${Spacing.base * 112}px;
  padding: 0 ${Spacing.base * 2}px;
`;

export default Wrapper;
